import { Typography } from '@material-ui/core';
import './styles.scss';

export const OrDivider = () => {
  return (
    <Typography className='or-divider'>
      <span>OR</span>
    </Typography>
  );
};
